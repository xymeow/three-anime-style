# 风格实验室

v0.4.1 已将早期试玩和实验室合并到同一个入口、同一份 `demo/main.ts`。`lab.html` 只负责旧链接跳转。

[在线实验室](https://xymeow.github.io/three-anime-style/?scene=robot&palette=ice&compare=shadows)

```sh
npm ci
npm run dev -- --port 5176
# http://127.0.0.1:5176/?example=fixture
npm run build:lab
```

## 场景

- 山地：宽阔连续曲面，观察大笔触怎样跟随光照。
- 峡谷：岩壁与小建筑，对照平滑和低多边形造型。
- 长廊：大墙面、柱体、拱面，开关投影看笔触是否被遮住。
- 几何体：球、二十面体、环结使用三段光照，背景使用连续笔触光照。
- Cesium Man：带贴图、蒙皮和行走动画的人形。
- Quaternius Character：多材质低模人物，24 个动作。
- Robot Expressive：43 根骨骼、14 个动作，可手动切换 Angry / Surprised / Sad 表情形变。

六套配色：中性日光、朱砂/靛蓝、高饱和/宝蓝、冰蓝/紫色、黑白墨稿、线上暖灰。配色改变程序化场景的材质、色阶和灯光；导入角色保留自身颜色与贴图，也可以去贴图看中性材质。页面统计包含背景舞台。

左右共用相机、模型和同一帧骨骼姿态，依次渲染。背景对照只改变笔触强度；原材质对照的左侧关闭三渲二与表面效果。初始颗粒、描边、磨砂均为零；可以一键恢复线上使用的 50% 颗粒、50% 磨砂与 1.1 px 描边。

## 实际观察

- 连续曲面和大面积岩壁能显示宽笔触；建筑平面上的效果比较轻，投影开启后尤其容易被遮住。背景默认关闭投影，人物默认开启。
- 换成朱砂/靛蓝或高饱和宝蓝后，仍然能形成清楚的色块关系。原来的暖灰观感不只来自三段色阶，场景材质和暖色灯光也起作用。
- 低模人物的头发、脸部与关节会出现更明确的块面。细节复杂并不自动变成精细的二维画风，法线和原贴图仍决定结果。
- 三种导入角色的骨骼动画正常；机器人表情在三渲二和描边下也可显示。面部特写下，50% 颗粒与磨砂已经很显眼，建议先清除表面效果再判断色阶。
- 这轮覆盖风格化人物和机械角色，还没有验证写实皮肤、头发透明层或高精度人脸。

## 验证

TypeScript、10 项现有测试、原 demo 构建、实验页构建、Prettier 检查。浏览器实际检查七个场景、不同配色、分面切换、去贴图、机器人表情、动画、磨砂与描边。

统一视图与场景注册位于 `demo/`，地形与接地影工具位于 `lab/`，模型和来源说明在 `public/lab-models/`。双视口会渲染两次，适合比较效果；此页没有做性能基准。

## 2026-09-19：描边修复与动画阴影

低角度时，原描边把平面上连续的视空间深度变化当成边界，钢笔压力扰动进一步产生棋盘状黑带。修复使用投影深度的局部斜率估计，采样对齐纹素中心，排除连续斜面；对象/材质边界与同一网格的真实自遮挡仍保留。

新增「普通投影 / 动画阴影」对照，两侧都有三渲二与描边，右侧可单独开关三种处理：

- 抽象接地影：取消角色的计算投影，用一块轮廓不完全规则的扁平色块接地。
- 片层偏移影：角色轮廓向屏幕右下偏移少量，模仿前照明赛璐璐片层的分离感。
- 暗部反光：在暗部轮廓附近混入少量中间色。

参考 [Shapefarm 原技术博客](https://www.unrealengine.com/tech-blog/stepping-inside-a-retro-anime-inspired-game-a-look-into-the-rendering-of-orbitals) 的暗部高光、前照明片层阴影与抽象接地贴花。这里用 Three.js 实现上述观感；接地影适配当前平舞台，未实现原作的烘焙体积光照或地形贴花投射。

初始钢笔线宽调整为 1.1 px，颗粒与磨砂仍为零。角色默认打开动画阴影；可切回普通投影。浏览器像素回归入口：`/tests/gpu.html`。

## 合并试玩

庭院、灯塔、维修机器人、牛油果、动画狐狸、骨骼与形变 fixture 与上面的七组场景共用渲染循环、材质转换、动画和阴影。支持本地 GLB、PNG 与参数导出；旧 `example=robot` 保留维修机器人，`scene=robot` 保留表情机器人。
